import { Container, Typography } from '@mui/material';

const RepairPage = () => {
    return (
        <Container>
            <Typography style={{ marginTop: 100, color: 'black' }}>
                Repair
            </Typography>
        </Container>
    );
};

export default RepairPage;